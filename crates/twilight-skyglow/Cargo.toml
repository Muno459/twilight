[package]
name = "twilight-skyglow"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Light pollution skyglow model for twilight prayer times"

[dependencies]
ureq = "3"
png = "0.17"
flate2 = "1"
twilight-core = { workspace = true }
twilight-data = { workspace = true }
# Shared HTTP retry and tile-cache helpers (retry::with_retries, cache::*).
twilight-weather = { workspace = true }

[dev-dependencies]
